-- Run these commands after setting up the project:
-- 1. dotnet ef migrations add InitialCreate
-- 2. dotnet ef database update
-- OR run this SQL directly on your MSSQL server after creating the DB:

-- The EF Core migrations will auto-create tables.
-- Make sure your connection string in appsettings.json points to your SQL Server.
-- Default admin credentials: admin / Admin@123
